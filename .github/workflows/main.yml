## \\ CI/CD Pipeline for Django to AWS Elastic Beanstalk #
name: CI/CD Pipeline to AWS Elastic Beanstalk deploy

on:
  push:
    branches: [ main ]

jobs:
  # --- LAYER 1: STATIC TESTING ---
  lint:
    name: Static Analysis (Pylint)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./incident-management
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pylint pylint-django

      - name: Run Pylint
        run: |
          export PYTHONPATH=$PYTHONPATH:$(pwd)
          pylint $(find . -name "*.py" -not -path "*/venv/*" -not -path "*/migrations/*") \
          --load-plugins pylint_django \
          --django-settings-module=config.settings \
          --fail-under=5.0

  sonarqube:
    name: SonarQube Analysis
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: SonarQube Scan
        uses: SonarSource/sonarqube-scan-action@v5
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.projectKey=aman7csk-cloud_incident-management
            -Dsonar.organization=aman7csk-cloud
            -Dsonar.sources=incident-management
            -Dsonar.python.version=3.11
            -Dsonar.exclusions=**/migrations/**,**/venv/**,**/.c9/**

  # --- LAYER 2: DEPLOYMENT ---
  deploy:
    name: Deploy to Elastic Beanstalk
    runs-on: ubuntu-latest
    needs: [lint, sonarqube] 
    defaults:
      run:
        working-directory: ./incident-management

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # Generates the staticfiles/ directory so CSS/JS works in production
      - name: Collect Static Files
        run: python manage.py collectstatic --noinput

      - name: Generate Deployment Package
        run: zip -r deploy.zip . -x "*.git*" "venv/*" ".c9/*"

      - name: Deploy to AWS Elastic Beanstalk
        uses: einaregilsson/beanstalk-deploy@v21
        with:
          aws_access_key: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_session_token: ${{ secrets.AWS_SESSION_TOKEN }} 
          application_name: "Incident_management"
          environment_name: "Incidentmanagement-env-1" 
          region: "us-east-1"
          version_label: "app-${{ github.sha }}"
          # CRITICAL FIX: The action needs the full path from the repo root
          deployment_package: "incident-management/deploy.zip" 
          use_existing_version_if_available: true


# \\ CI/CD Pipeline for Django to AWS Elastic Beanstalk #
